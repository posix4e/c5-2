machine:
  java:
    version: oraclejdk8
dependencies:
  cache_directories:
    - ~/.m2
test:
  override:
    - mvn:
        parallel: true
        files:
          - case $CIRCLE_NODE_INDEX in 0) mvn test ;; 1) mvn test-compile failsafe:integration-test ;; esac:
